name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Validate repository structure and question banks
        run: python tools/validate_repo.py
      - name: Run unit and negative-fixture tests
        run: python -m unittest discover -s tests -v
      - name: Check Python syntax
        run: python -m compileall -q tools tests
